import {View, Text} from 'react-native';
import React from 'react';

function Configuracoes() {
  return (
    <View
      style={{
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        minHeight: '100%',
        minWidth: '100%',
      }}>
      <Text style={{fontSize: 32}}>Configuracoes</Text>
    </View>
  );
}

export default Configuracoes;
